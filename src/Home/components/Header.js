// src/Home/components/Header.js

import React from "react";
import "./Header.css"; // Assume a corresponding CSS file for styling

const Header = () => {
    return (
        <header className="header">
            <h1>My Online Shop</h1>
            // Other header content
        </header>
    );
};

export default Header;
